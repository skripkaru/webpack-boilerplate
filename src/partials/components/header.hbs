<header class="header">
  <div class="container">
    <div class="header__inner">
      <a href="{{logo.url}}">
        <img width="40" src="{{logo.image}}" alt="" />
      </a>
      <nav class="flex gap-xs">
        {{#each pages }}
        <a href="{{ url }}">{{ title }}</a>
        {{/each }}
      </nav>
    </div>
  </div>
</header>
